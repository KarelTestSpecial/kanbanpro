# name: Java & React CI

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# jobs:
#   build-backend:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check out code
#         uses: actions/checkout@v4

#       - name: Set up JDK 17
#         uses: actions/setup-java@v4
#         with:
#           java-version: '17'
#           distribution: 'temurin' # Een populaire, open-source distributie van Java
#           cache: 'maven'

#       - name: Build and test with Maven
#         run: mvn -B clean install # -B voor non-interactieve mode
#         working-directory: ./backend

#   test-frontend:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check out code
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: 'npm'
#           # Zorg ervoor dat de cache wordt ge-invalideerd als package-lock.json verandert
#           cache-dependency-path: frontend/package-lock.json

#       - name: Install dependencies cleanly
#         run: npm ci
#         working-directory: ./frontend

#       - name: Run tests
#         run: npm test
#         working-directory: ./frontend
